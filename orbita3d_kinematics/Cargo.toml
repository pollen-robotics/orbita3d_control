[package]
name = "orbita3d_kinematics"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
log = "0.4.20"
levenberg-marquardt = "0.14.0"
nalgebra = "0.33.2"
ndarray = "0.16.1"
ndarray_einsum_beta = { git = "https://github.com/bangconghuynh/einsum.git" }
nshare = "0.10.0"
serde = { version = "1.0.183", features = ["derive"] }

[dev-dependencies]
rand = "0.8.5"
